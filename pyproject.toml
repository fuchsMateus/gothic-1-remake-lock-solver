[project]
name = "lockpic-gothic-1-remake"
version = "0.1.0"
description = "Desktop solver and keyboard executor for the Gothic 1 Remake lock puzzle"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
